<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>건크회원정보</title>
  <link rel="stylesheet" href="/styles/board.css">
</head>
<body>
  <div class="board-container">
    <h1 class="board-title">📋 정회원(연도별)</h1>
    <a class="btn" href="/">돌아가기</a>

    <form method="get" action="/content/ka_010100" class="year-form">
      <label for="year">연도:</label>
      <input type="number" name="year" id="year" value="<%= selectedYear %>" placeholder="예: 2025" />
      <button type="submit">검색</button>
    </form>

    <table class="board-table">
      <thead>
        <tr>
          <th>번호</th>
          <th>회원명</th>
          <th>회원등급</th>
          <th>성별</th>
          <th>닉네임</th>
          <th>비고</th>
        </tr>
      </thead>
      <tbody>
        <% if (users.length === 0) { %>
          <tr>
            <td colspan="4">해당 연도의 회원이 없습니다.</td>
          </tr>
        <% } else { %>
          <% users.forEach((user, idx) => { %>
            <tr>
              <td><%= idx + 1 %></td> <!-- 번호 붙이기 -->              
              <td><%= user.user_nm %></td>
              <td><%= user.member_class_nm %></td>
              <td><%= user.gender %></td>
              <td><%= user.user_nm_alias %></td>
              <td><%= user.remark %></td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>
  </div>

  <script src="javascripts/script.js"></script>
</body>
</html>